<%= turbo_stream.append "modals" do %>
  <div class="fixed inset-0 z-50 overflow-y-auto" 
       data-controller="modal"
       data-action="keydown.esc@window->modal#close">
    <div class="fixed inset-0 bg-slate-900/40 backdrop-blur-sm transition-opacity"
         aria-hidden="true"
         data-action="click->modal#close"></div>
    <div class="fixed inset-0 flex items-center justify-center p-4 pointer-events-none">
      <div class="admin-surface rounded-xl p-6 max-w-lg w-full relative pointer-events-auto" 
           data-modal-target="content">
        <div class="flex justify-between items-center mb-4">
          <h3 class="text-lg font-semibold text-slate-900">Password Reset Link</h3>
          <button data-action="click->modal#close" class="text-slate-400 hover:text-slate-600 cursor-pointer">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        <p class="text-sm text-slate-600 mb-4">Send this link to <strong><%= @user.email_address %></strong>:</p>
        <div class="flex gap-2">
          <input type="text" 
                 value="<%= @reset_url %>" 
                 readonly 
                 id="reset-link-<%= @user.id %>"
                 class="flex-1 p-3 border border-slate-300 rounded-md bg-white/80 text-sm font-mono text-slate-700">
          <button onclick="navigator.clipboard.writeText(document.getElementById('reset-link-<%= @user.id %>').value); this.textContent = 'Copied!'; setTimeout(() => this.textContent = 'Copy', 2000)"
            class="admin-btn-primary whitespace-nowrap cursor-pointer">
            Copy
          </button>
        </div>
        <p class="text-xs text-slate-500 mt-3">Link expires in 48 hours.</p>
      </div>
    </div>
  </div>
<% end %>
